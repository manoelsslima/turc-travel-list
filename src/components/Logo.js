/* when using "default", we can give any name in the import.
For example, in App.js, we can import this component (without { }):

import xpto from "./Logo"

because the default component is Logo
*/
export default function App() {
  return <h1>🌴 Far Away 👜</h1>;
}
